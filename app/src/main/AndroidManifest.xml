<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="de.juliushetzel.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_vagar_example"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_vagar_example"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="de.juliushetzel.sample.view.TaskListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".view.AddTaskActivity">
        </activity>
    </application>

</manifest>